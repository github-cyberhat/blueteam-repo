Change SSH default port

nano /etc/ssh/sshd_config

Remove comment of port 22 and replace port number

Verify listening port of ssh
netstat -tulpn | grep ssh

service sshd restart


#Hardening SSH
https://github.com/WGU-CCDC/Blue-Team-Tools/tree/main/Linux/Harden-SSH

